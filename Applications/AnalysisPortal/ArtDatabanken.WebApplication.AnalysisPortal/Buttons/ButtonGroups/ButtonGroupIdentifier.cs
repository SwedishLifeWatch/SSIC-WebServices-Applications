namespace ArtDatabanken.WebApplication.AnalysisPortal.Buttons.ButtonGroups
{
    /// <summary>
    /// This enum identifies the button groups used primarily on the main page (they are also used elsewhere).
    /// </summary>
    public enum ButtonGroupIdentifier
    {
        None,
        Overview,
        DataProviders,
        Filter,        
        Calculation,        
        Presentation,
        Result,
        Settings
    }
}
